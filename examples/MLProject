name: Mnist Example
experiemnt name: test

conda_env: mlflow/conda_env.yaml

entry_points:
  main:
    parameters:
      config_file: path
    command: "tfexp fit {config_file}; tfexp evaluate {config_file}"
